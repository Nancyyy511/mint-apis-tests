## Base URL
# Used if not overridden by:
#  - system property: -DbaseUrl=...
#  - env var: MINT_BASE_URL
baseUrl=https://mint-preprod.cairocapitalgroup.com

## Default headers (applied to all requests)
# Any property starting with headers. becomes a default header.
headers.min-ios-version=1021
headers.imei=postman-test
headers.MOBILEIP=192.168.100.46
headers.Accept-Language=en-US
headers.lang=en

## Local-only fallback credentials (DO NOT commit real credentials here)
# These are used only when the matching environment variables are not set:
#  MINT_USERNAME, MINT_PASSWORD, MINT_PIN, MINT_SECURITY_ANSWER
mint.username=012282349004
mint.password=@Testing09
mint.pin=0000
mint.securityAnswer=1

## Auth behavior
auth.useBiometric=1
//auth.useBiometric=0
